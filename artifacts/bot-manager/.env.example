# No backend secrets needed — admin key is entered in the UI.
# For Render static site:
# Build command: npm install && npm run build
# Publish directory: dist
# BASE_PATH=/   (set to /bot-manager/ if serving under a sub-path)
